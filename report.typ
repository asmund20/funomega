#set document(title: [Implementation of the $"Fun"_omega$ language])
#set heading(numbering: "1.1")
#set par(justify: true)
#set page(numbering: "1")
#show "funomega": $"Fun"_omega$

#align(center)[
  #title()
  #datetime.display(datetime.today())
]

// #outline(depth: 2)

I have not gotten too far in my implementation of the funomega language. I think
I have a working parser, but have only verified it against the programs in the
`examples` folder. I have not had the time to fully implement the interpreter.

This is obviously not even a first draft for the sections on the parser and
interpreter, so I do not expect any feedback on my "report".

If you could look at the driver and interpreter source files, that would be
appreciated.
